const initialState = {
  validWords:[],
  invalidWords:[],
}

const wordListReducer = (wordList = initialState, action) => {
  switch (action.type) {
    default:
      return wordList
  }
}

export default wordListReducer