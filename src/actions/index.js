import * as fromBoard from './board'
import * as fromWord from './word'
import * as fromWordList from './word-list'

export const board = fromBoard
export const word = fromWord
export const wordList = fromWordList